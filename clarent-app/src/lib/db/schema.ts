// Drizzle schema — Clarent Environmental
//
// All tables defined here. Source of truth for runtime types and the query
// builder. SQL migrations live in `supabase/migrations/` and are applied via
// the Supabase CLI (see memory: feedback_clarent_migrations.md).
//
// Phase 1 will populate this file with the full schema from the project plan.
// For now it's a placeholder so the client compiles.

export {};
